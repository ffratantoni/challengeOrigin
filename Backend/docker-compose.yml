version: '3.8'
services:
  backend:
    build: .
    ports:
      - '8000:8000'
    volumes:
      - ./:/app:delegated
    environment:
      - PYTHONUNBUFFERED=1
    command: sh -c "python -m app.init_db && uvicorn app.main:app --host 0.0.0.0 --port 8000"
